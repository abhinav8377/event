# Backend API base URL. Leave empty to use the Vite dev proxy (/api -> http://localhost:5000).
VITE_API_URL=

# Clerk publishable key for "Sign in with Google" (starts with pk_).
# Get it from the Clerk dashboard: https://dashboard.clerk.com
# Leave empty to hide the "Continue with Google" button (email/password login still works).
VITE_CLERK_PUBLISHABLE_KEY=pk_test_ZGVsaWNhdGUtY29icmEtNC5jbGVyay5hY2NvdW50cy5kZXYk
